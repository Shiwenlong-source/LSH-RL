# Examples

This compact reproduction fork keeps only one example:

- `lsh-rl/`: dialogue-RL and action-RL roleplay training flows.

Run all commands from the repository root unless a script says otherwise.
